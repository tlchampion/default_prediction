from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Prediction if an individual will default on their credit card',
    author='Thomas Champion',
    license='MIT',
)
